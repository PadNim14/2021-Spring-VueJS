const app = new Vue({
    el: '#app',
    data: {
        message: "I changed it.",
        other_message: "Variables.",
    }
});

const newapp = new Vue({
    el: '#newapp',
    data: {
        mess1: "Here is more text"
    }
});
